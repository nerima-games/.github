# nerima-games 決定記録

nerima-games org の決定を記録する場所です。
第1部が記録の運用規約、第2部がこれまでに下された決定の索引と本体です。

調査日は 2026-08-01 です。この文書自体、[GLOSSARY.md](GLOSSARY.md) と並んで、この org にこれまで
存在しなかった新規文書です。他の10個の標準文書（[CONTRIBUTING.md「標準文書」](CONTRIBUTING.md#標準文書)
の一覧を参照）の多くが既存の実践を書き起こしたものであるのに対し、この文書と GLOSSARY.md は
今回の org 標準化作業そのものの過程で初めて必要になった、性質の異なる2文書です。

## 第1部: 決定記録の運用規約

### この文書の役割

[PACKAGE_STANDARD.md](PACKAGE_STANDARD.md) をはじめとする各標準文書は「今何が正しいか」を書きます。
この文書は「なぜそう決めたか、何を捨てたか」を書きます。

標準文書だけがある状態では、決定に不満を持った人が根拠を追えません。根拠が追えないと、
同じ議論が別のリポジトリや別のセッションで繰り返されます。nerima-lisp org の
`DECISIONS.md` が同じ理由でこの形式を採っており、本書はその運用規約を踏襲します。ただしこの
org は標準文書が12個と、nerima-lisp（21リポジトリ、より広いスコープの規約群）より小さいため、
規約自体はより軽量にします。

### 記録すべき決定

次のいずれかに当てはまるものを記録します。

- **複数のリポジトリに波及する。** 16リポジトリのうち2つ以上の構成やコードが変わるなら記録します。
- **後から覆すのが高価。** 覆すために16リポジトリの一括変更や、下流の追随が要るなら記録します。
- **選択肢が複数あり、片方を捨てた。** 採らなかった案が存在するなら記録します。捨てた案を
  書き残さないと、それが再提案されます。

### 記録しなくてよい決定

- 1リポジトリの内部実装（関数の分割、アルゴリズムの選択）
- 標準文書の言い換え、誤字修正、リンク切れの修正
- 個別リポジトリのリリース判断、バージョン番号そのものの決定
- measurement の更新（カバレッジ実測値の変動など）。これは各標準文書の「既知の非適合」節が持ちます

判断に迷ったら記録してください。記録しすぎたときの損は索引が長くなることだけですが、
記録しなかったときの損は議論のやり直しです。

### 置き場所はこの単一ファイルにします

`.github/decisions/NNNN-title.md` のような連番ファイルには**しません**。この `DECISIONS.md`
に追記していきます。

理由は nerima-lisp と同じです。この org もメンテナが1人であり、決定の追加頻度は低く、
索引を本体と別ファイルに保つと更新が忘れられて食い違います。単一ファイルなら索引と本体が
同じコミットでしか動きません。加えて、12個の標準文書という規模ではファイル分割の利点
（複数人が同時編集しても衝突しない）が働きません。

分割する条件は、この文書が800行を超えるか、記録が60件を超えたときとします（nerima-lisp の
1500行・150件という上限より小さいのは、この org の標準文書自体が12個と少なく、決定の総数も
それに比例して小さく収まる見込みのためです）。それまでは分割しません。

### 1件の記録の書式

```markdown
#### DEC-NNNN 決定を1行で述べた表題

日付 YYYY-MM-DD ／ 状態 採用

- **文脈** なぜこれを決める必要があったのかを1〜3文で
- **決定** 何をすると決めたか。断定形で書く
- **根拠** なぜその選択にしたか
- **捨てた選択肢** 検討して採らなかった案。なければ「なし」
- **影響** どの標準文書・どのリポジトリに何が波及するか
```

1件あたり10〜25行に収めます。記録は第2部で群ごとにまとめ、群の見出しを `###`、記録の見出しを
`####` にします。

**表題は決定の内容を書きます。** 「カバレッジについて」ではなく「カバレッジゲートを即時・全リポジトリで有効化する」
と書きます。索引の一覧表がそのまま決定の一覧になるようにするためです。

### ID は連番にします

`DEC-0001` から始まる4桁の連番です。欠番を作らず、削除した番号を再利用しません。

### 決定を覆すときは新しいエントリを追加します（既存のエントリは書き換えません）

**決定を覆すときは既存の記録を書き換えません。新しい記録を作り、古い方の状態を
「置換済み（DEC-NNNN により）」に変えます。** 変更するのは状態の行だけで、文脈と決定と根拠の
本文はそのまま残します。

古い記録を書き換えてしまうと、当時どういう情報で何を判断したのかが失われます。判断が
間違っていたことを示す記録こそ、同じ間違いを防ぎます。新しい記録の「文脈」には、古い決定の
どこが実態と合わなかったかを書いてください。

状態は4つです。

| 状態 | 意味 |
|---|---|
| 提案中 | Pull Request が開いている。まだ従う義務はない |
| 採用 | マージ済み。標準文書に反映されており、従う義務がある |
| 却下 | 提案されたが採らなかった。再提案の前に読む対象 |
| 置換済み | 別の記録に置き換わった。置換先の ID を明記する |

遷移は次の3経路だけです。

```
提案中 → 採用
提案中 → 却下
採用   → 置換済み（DEC-NNNN により）
```

### 決定を提案する手続き

提案先は `nerima-games/.github` リポジトリの Pull Request です。

1. この文書に「提案中」の記録を追加します。ID は既存の最大値の次を使います。記録だけの PR は
   作りません。次の2点と同じ PR にまとめます。
2. 該当する標準文書（[CONTRIBUTING.md「標準文書」](CONTRIBUTING.md#標準文書)の一覧のいずれか）を
   同じ PR で書き換えます。標準文書が「今何が正しいか」を示す唯一の場所なので、記録だけ足して
   標準文書を直さない状態を作りません。
3. 16リポジトリへの移行が必要なら、影響欄にどのリポジトリが対象かを明記します。この org には
   nerima-lisp の `MIGRATION_RUNBOOK.md` に相当する専用文書はまだないため、移行手順そのものは
   影響欄か、対象リポジトリの `docs/` に書きます。

マージした時点で状態を「採用」に変えます。提案が採られなかった場合は、状態を「却下」にした
記録だけを別 PR でマージします。

### 決定を蒸し返さないための規約

作業中に決定へ不満を持ったときの正しい経路は、作業を止めることではありません。この文書に
対する Pull Request を出すことです。

1. 手元の作業は、現行の決定に従って完了させます。
2. 不満の内容を、新しい記録の「文脈」として書きます。
3. 上の提案手続きに沿って PR を出します。
4. その PR がマージされてから、影響を受けるリポジトリを直します。

作業を止めて議論を始めると、16リポジトリのうち一部だけが古い基準、一部が新しい基準、残りが
未着手という3状態になります。

**例外は1つだけです。** 決定に従うと `pnpm verify` や CI のゲートが通らない場合は、作業を止めて
利用者に確認してください。これは不満ではなく、決定と実態の矛盾です。

---

## 第2部: これまでの決定の索引

以下は今回の org 標準化セッション（2026-08-01）で実際に下された決定です。すべて同日に
まとめて記録されたものであり、下された順序と記録された順序は一致します。

### 一覧表

| ID | 表題 | 状態 |
|---|---|---|
| DEC-0001 | 12文書のフルセット（nerima-lisp 方式）を採用し、段階的な最小 MVP は採らない | 採用 |
| DEC-0002 | `src/` ディレクトリを新設し `index.ts` / `domain/` / `application/` / `stages/` を集約する（`apps/` は対象外） | 採用 |
| DEC-0003 | `api-lock.md` / `scripts/api-lock.ts` を全廃し、自動 API スナップショット機構を持たない | 採用 |
| DEC-0004 | `scripts/check-dependency-whitelist.ts` を廃止し、oxlint の `no-restricted-imports` に置き換える | 採用 |
| DEC-0005 | カバレッジ4指標99%ゲートを、ロールアウト当日から即時・全リポジトリで有効化する | 採用 |
| DEC-0006 | リリースバージョニングに changesets を採用し、日数計測ベースの凍結ゲートを廃止する | 採用 |
| DEC-0007 | `REVIEW_STANDARD.md` をピアレビューではなくセルフレビュー/エージェント委譲としてモデル化する | 採用 |

以下、群ごとに本体を並べます。

### 標準化の進め方

#### DEC-0001 12文書のフルセット（nerima-lisp 方式）を採用し、段階的な最小 MVP は採らない

日付 2026-08-01 ／ 状態 採用

- **文脈** nerima-games には org 横断の標準文書が1つも存在しなかった。姉妹 org である
  nerima-lisp にはすでに `PACKAGE_STANDARD.md` / `CODING_STANDARD.md` / `DECISIONS.md` などの
  文書群があり、21リポジトリを1つの基準で揃えた実績があった。
- **決定** nerima-lisp をモデルとした12文書のフルセット（[CONTRIBUTING.md「標準文書」](CONTRIBUTING.md#標準文書)
  が列挙する `PACKAGE_STANDARD.md` `CODING_STANDARD.md` `API_STANDARD.md` `TEST_STANDARD.md`
  `DOCS_STANDARD.md` `PERFORMANCE_STANDARD.md` `DEPENDENCY_POLICY.md` `RELEASE_STANDARD.md`
  `SUPPLY_CHAIN.md` `REVIEW_STANDARD.md` `GLOSSARY.md` `DECISIONS.md`）を、最初から org 標準として
  一括採用する。
- **根拠** 16リポジトリという規模はすでに、暗黙の合意だけで揃えるには大きい。まず1〜2文書の
  最小構成で始めて後から足す進め方は、足す作業が「今からでも間に合う」うちに先送りされ続ける
  リスクがある一方、フルセットを最初に書き切れば、以後の標準化はこの12文書への追記・改訂に
  一本化できる。
- **捨てた選択肢** 段階的な最小 MVP（例えば `PACKAGE_STANDARD.md` と `CODING_STANDARD.md` だけを
  先に書き、他は必要になってから足す進め方）。
- **影響** 全16リポジトリ、および `.github` リポジトリ自身。12文書のうち `DEPENDENCY_POLICY.md`
  は本セッション時点でまだ未執筆（他の文書から「未執筆」として参照されている）であり、
  フルセット採用の決定と、実際に12文書すべてが書き終わっていることは別である。

### パッケージ構成

#### DEC-0002 `src/` ディレクトリを新設し `index.ts` / `domain/` / `application/` / `stages/` を集約する（`apps/` は対象外）

日付 2026-08-01 ／ 状態 採用

- **文脈** 現状（`mc-kernel` `mc-render` `mx-ui` いずれも）は `index.ts` と `domain/`（該当すれば
  `application/` `stages/`）がリポジトリ直下に、`test/` `scripts/` `apps/` `docs/` などの
  非配布ディレクトリと同じ階層に並んでいた。「配布されるコード」と「配布されないコード」が
  ディレクトリ階層の上では区別できず、`package.json#files` が個別ファイル・ディレクトリの列挙で
  ようやく境界を表現している状態だった。
- **決定** `src/` を新設し、配布対象（`index.ts` `domain/` `application/` `stages/`）をすべて
  その下に集約する。`apps/`（プレビュー/デモの実行エントリ）は `src/` の外、リポジトリ直下の
  きょうだいのまま維持し、`src/` の中には入れない。
- **根拠** `src/` に集約すれば `package.json#files` は `"src"` の一語、oxlint の対象パスは
  `src` 一語、`tsconfig.build.json` の `include` は `src/**/*.ts` の一語で表現できる。`apps/` を
  対象外にするのは、`tsconfig.preview.json` が証明する「配布物は DOM/three に依存しない」という
  主張の対象外にプレビュー/デモを置くための切り分けであり、配布対象の境界を「`src/` の内か外か」
  という1つの問いに単純化するため。
- **捨てた選択肢** `apps/` も含めて `src/` 配下に入れる案（配布物の境界がさらに曖昧になるため
  不採用）。現状維持（個別ファイル・ディレクトリ列挙のまま）。
- **影響** 全16リポジトリの `package.json`（`main` / `types` / `exports` / `files` / `scripts.lint`
  等）、`tsconfig.*.json` の5ファイル、`vitest.config.ts` の `coverage.include`。詳細は
  [PACKAGE_STANDARD.md「なぜ `src/` か」](PACKAGE_STANDARD.md#なぜ-src-か)。本書執筆時点で
  この目標状態になっているリポジトリはまだ存在しない。

### API とツール

#### DEC-0003 `api-lock.md` / `scripts/api-lock.ts` を全廃し、自動 API スナップショット機構を持たない

日付 2026-08-01 ／ 状態 採用

- **文脈** `mc-kernel` `mc-render` `mx-ui` は `api-lock.md` と `scripts/api-lock.ts`
  （および `package.json` の `api:check` / `api:update` スクリプト）を持ち、CI で `pnpm api:check`
  により検査し、「4週間 API 無変更で凍結」という1.0.0 リリースゲートに紐づけていた。導入前に
  `@microsoft/api-extractor` も評価されたが、`Context.Tag`（例:
  `ClockPort = Context.Tag('@nerima-games/mc-kernel/ClockPort')`）の宣言が生成する非 export の
  基底クラス（`ClockPort_base`）を「forgotten export」として捨ててしまい、Tag 識別子文字列を
  改変してもレポートがバイト単位で同一のままになる欠陥が実測されていた。
- **決定** `api-lock.md` / `scripts/api-lock.ts` を org 標準から完全に削除する。今後どの
  リポジトリでも自動 API スナップショット/diff ツールを必須としない。公開 API は
  `src/index.ts` が re-export するものそのものと定義し（[API_STANDARD.md §1](API_STANDARD.md#1-公開-apiの定義)）、
  破壊的変更の判定は人間のレビューで行う。
- **根拠** 自前の `api-lock.ts` 機構も `@microsoft/api-extractor` も、Tag 識別子文字列という
  「型検査では検出できないが実行時に壊れる」変更を捉えられないという同種の欠陥を持っていた。
  ツールが検出できない変更を「検出したことにする」仕組みを維持するコストが、その仕組みが
  実際に防いでいる事故の量に見合わないと判断した。
- **捨てた選択肢** `@microsoft/api-extractor` の新規採用。自前 `api-lock.ts` 機構の改修による
  存続。
- **影響** `mc-kernel` `mc-render` `mx-ui` の `api-lock.md` / `scripts/api-lock.ts` /
  `package.json` の `api:check` `api:update` `check:deps` を伴う `verify` 定義。`verify` は
  `typecheck && lint && test` の3段に縮小される（[TEST_STANDARD.md §1](TEST_STANDARD.md#1-pnpm-verify-の構成)）。
  破壊的変更の判定基準そのものは [API_STANDARD.md §3](API_STANDARD.md#3-破壊的変更-vs-加算的変更)
  が定める。

#### DEC-0004 `scripts/check-dependency-whitelist.ts` を廃止し、oxlint の `no-restricted-imports` に置き換える

日付 2026-08-01 ／ 状態 採用

- **文脈** `scripts/check-dependency-whitelist.ts` は Tier 間の依存許可リストと、時刻源の直接
  呼び出し禁止のようなコード規約の両方を1本のカスタムスクリプトで検査していた。
- **決定** `scripts/check-dependency-whitelist.ts` を org 標準から削除する。代替は各リポジトリの
  `oxlint.json` に書く `no-restricted-imports` ルールとする。`mc-kernel/oxlint.json` はすでに
  `no-restricted-imports` で `effect` のデフォルトインポート禁止を書いており、これを拡張して
  モジュール間の禁止 import をここへ移す。**内容はリポジトリごとに違ってよく、むしろ違うべき**
  である。oxlint のルールで表現できないチェック（時刻源の直接呼び出し禁止など）は、oxlint が
  そのルールを実装するまでの間、当該リポジトリの `scripts/` に個別の代替スクリプトを置くかどうかを
  各リポジトリの裁量とし、org 標準としては要求しない。
- **根拠** 標準の lint 設定ファイル（`oxlint.json`）に寄せることで、依存禁止ルールがカスタム
  スクリプトという「もう1つの読む場所」を持たずに済む。byte-identical な設定を全16リポジトリに
  強制することは適合の条件にしない。
- **捨てた選択肢** `check-dependency-whitelist.ts` を維持したまま改修する案。全リポジトリで
  完全に同一の `oxlint.json` を強制する案（許可された依存先は各リポジトリで異なるため不採用）。
- **影響** 全16リポジトリの `oxlint.json` と `package.json` の `scripts.verify`（`check:deps`
  ステップの削除）。Tier 間の依存許可そのものの内容は `DEPENDENCY_POLICY.md`（本書執筆時点で
  未執筆）が扱う。

### テストとカバレッジ

#### DEC-0005 カバレッジ4指標99%ゲートを、ロールアウト当日から即時・全リポジトリで有効化する

日付 2026-08-01 ／ 状態 採用

- **文脈** vitest `--coverage`（provider: v8）による statements / branches / functions / lines
  の閾値設定は、`mc-kernel` など一部リポジトリではすでに `thresholds: { branches: 99, functions: 99,
  lines: 99, statements: 99 }` が有効だったが、org 全体では統一されていなかった。実測の結果、
  `mc-audio`（functions 84.12%）、`mc-compose`（ベースライン未計測）、`mc-playground-kit`
  （functions 96.29%）の3リポジトリが、閾値を有効化した瞬間に CI が赤くなることが判明した。
- **決定** 4指標すべてで99%のしきい値を、猶予期間や段階的ロールアウトを伴わず、全16リポジトリの
  CI にロールアウト当日から即時・一律に適用する。`mc-audio` / `mc-compose` / `mc-playground-kit`
  がロールアウト日に CI 赤になることは想定内として受け入れ、しきい値を緩める理由にはしない。
- **根拠** 「まず一部リポジトリから」「まず90%から始めて段階的に上げる」といった移行計画は、
  未達リポジトリの是正が先送りされ続けるリスクを持つ。3リポジトリの未達は
  [TEST_STANDARD.md §4](TEST_STANDARD.md#4-既知の非適合ロールアウト時点)に既知の非適合として
  列挙し、追跡対象の作業として扱うことで、ゲートを緩めずに現状を可視化できる。
- **捨てた選択肢** 90%から段階的に引き上げる移行計画。リポジトリごとに異なる閾値を設定する案。
  カバレッジ実測値ぴったりにしきい値をピン留めする案（無関係なリファクタで容易に赤くなるため
  99% とし、1%の余白を残す）。
- **影響** 全16リポジトリの `vitest.config.ts`。`mc-audio` `mc-compose` `mc-playground-kit` の
  3リポジトリは CI が赤くなることが確定しており、[REVIEW_STANDARD.md §4](REVIEW_STANDARD.md#4-ci-待ちは-self-merge-の絶対条件)
  はこの3リポジトリに限り、known-red の required check を抱えたまま self-merge する場合の
  例外運用（PR 説明欄への明記を必須とする）を定めている。

### リリースとバージョニング

#### DEC-0006 リリースバージョニングに changesets を採用し、日数計測ベースの凍結ゲートを廃止する

日付 2026-08-01 ／ 状態 採用

- **文脈** 全16リポジトリを検証した結果、リリース基盤は何もなかった（`.changeset/` ディレクトリ、
  `@changesets/cli`、`CHANGELOG.md` のいずれも存在せず、`ci.yaml` に publish ジョブも無かった）。
  一方 `publishConfig`（GitHub Packages、`access: "restricted"`）は先行して全リポジトリに
  設定済みだった。旧来は「`api-lock.md` が4週間変更されなければ API は凍結されたとみなし、
  1.0.0 に昇格する」という日数計測ベースの自動ゲートが想定されていたが、`api-lock.md` 自体が
  DEC-0003 で廃止された。
- **決定** 全15対象リポジトリ（`mc-dev-meta` を除く。`"private": true` かつ "Never published."
  と明示された非公開パッケージのため）に `@changesets/cli` を導入し、PR ごとの `.changeset/*.md`
  追加とリリース PR（"Version Packages"）を経たバージョン bump・CHANGELOG 生成を単一の入り口とする。
  0.x → 1.0.0 への昇格は、日数や利用実績のような定量的な代替ゲートを設けず、**maintainer（take）
  による裁量判断のみ**で行う。
- **根拠** `publishConfig` がすでに GitHub Packages を指しており変更コストがかからないこと、
  GitHub Actions 組み込みの `GITHUB_TOKEN` だけで publish が完結すること（追加シークレット不要）。
  昇格ゲートを裁量判断にするのは、Terraform 定義ファイル `repos_nerima_games.tf` が示す
  「上の階層が実際にそのパッケージを消費し、動作確認を終える」という実質的なトリガーを、
  数値化された代替基準に落とし込むこと自体を避けるため。数値基準は「基準さえ満たせば中身を
  見なくてよい」という誤った安心感を生みやすい。
- **捨てた選択肢** 日数計測ベースの自動凍結ゲートの存続・改修。「利用実績◯件」のような定量的な
  代替昇格基準の新設。npmjs.com など GitHub Packages 以外への公開。
- **影響** 全15公開対象リポジトリ（`mc-dev-meta` を除く）の `devDependencies`、`ci.yaml` への
  publish ジョブ新設、リリース伝播順序（Tier1 → Tier2 → Tier3 → Tier4）。詳細は
  [RELEASE_STANDARD.md](RELEASE_STANDARD.md)。

### レビュー体制

#### DEC-0007 `REVIEW_STANDARD.md` をピアレビューではなくセルフレビュー/エージェント委譲としてモデル化する

日付 2026-08-01 ／ 状態 採用

- **文脈** nerima-games のブランチルールセットは `pull_request` ルール（承認必須）を要求しない。
  `private-terraform` の `rulesets.tf` によれば、これは以前 `cl-weave` で実際に踏んだ制約への
  対応であり、GitHub は self-approval を承認としてカウントせず、`gh pr merge --admin` のような
  bypass も ruleset には存在しない。1人しかいない org で承認必須を設定すると、そのリポジトリの
  最初の PR から誰も merge できなくなる。
- **決定** `REVIEW_STANDARD.md` は、ピアレビュー（複数人による分業）ではなく、著者自身（人間
  またはエージェント）によるセルフレビュー・自己マージをレビューモデルとして定める。GitHub の
  ruleset には表現できない「差分を全部読む」「レビューチェックリストを埋める」「CI を待つ」を、
  人間/エージェント向けのプロセス文書として運用する。エージェントが著者の場合は、実際に
  コマンドを実行して確認したことと、検証済み/未検証の範囲を PR 説明欄に明示することを追加で
  要求する。
- **根拠** GitHub の技術的制約（self-approval が承認としてカウントされない、ruleset に admin
  bypass が存在しない）が、1人+エージェント運用の org でピアレビュー必須ルールを設定すると
  破綻することを直接示している。レビューが本来担うべき条件を GitHub 側の自動ゲートではなく
  プロセス文書側に置くことで、ruleset の限界を回避する。
- **捨てた選択肢** `required_approving_review_count >= 1` を維持したまま、緊急時のみ admin
  bypass を使う運用（ruleset にはそもそも admin bypass の仕組みがなく、技術的に不可能）。
  レビュー基準を設けず self-merge を無条件に許可する運用（CI 待ちや差分確認を省略する余地を
  残すため不採用）。
- **影響** 全16リポジトリのブランチルールセット（`required_signatures` と `non_fast_forward`
  のみを GitHub 側の機械的ゲートとして残す）。CI が要求するカバレッジ99%ゲート（DEC-0005）に
  関して、`mc-audio` / `mc-compose` / `mc-playground-kit` の3リポジトリに限る known-red
  self-merge の例外運用も本決定の一部として `REVIEW_STANDARD.md` §4 に定める。
