# セキュリティポリシー

このファイルは org 全体のデフォルトです。自前の `SECURITY.md` を持たない
nerima-games 配下の全リポジトリに適用されます。

## 脆弱性の報告方法

セキュリティ上の問題を公開の issue に書かないでください。

対象リポジトリの GitHub Private Vulnerability Reporting を使ってください。
リポジトリの **Security** タブ → **Report a vulnerability** から報告できます。
これが最優先の報告経路です。

この経路が使えない場合は、`bararararatty@gmail.com` 宛にメールで報告してください。
件名に `[security]` とリポジトリ名を含めてください。

報告には、対象リポジトリとバージョン(またはコミット)、想定される影響、再現手順のうち
できるだけ小さいものを含めてください。`pnpm verify` が失敗する再現や、短いスクリプトでの
再現があると理想的です。

## 対応について

nerima-games は単独メンテナーによる開発体制です。約束できる内容は控えめです。

1週間以内の一次確認と、確認後の深刻度評価・修正見込みの提示を目標とします。
特に希望がない限り、リリースノートで報告者を謝辞に記載します。

## サポート対象バージョン

各リポジトリの最新リリースタグのみが修正対象です。長期サポートブランチはありません。
org 内のリポジトリはリリースタグでお互いを固定するため、修正は依存元リポジトリをタグ付けし、
依存先リポジトリでそのタグへ固定を更新することで伝播します。

## 対象範囲

nerima-games は TypeScript / Effect-ts で書かれた Minecraft クローンの再実装であり、
ネットワークサービスとして公開運用されているものではありません。報告として特に価値があるのは、
信頼境界をまたぐ失敗モードです。

- セーブデータや外部入力の読み込みで、無制限なメモリ確保・無限ループ・任意コード実行を
  引き起こしうるパーサ。`mc-save` のセーブフォーマット読み込み、`mc-worldgen` の
  ワールドデータ処理が該当します。
- `mx-multiplayer` のワイヤプロトコル・フレームコーデック・接続状態機械など、
  信頼できないピアからの入力を処理する経路。
- CI ワークフローの設定不備により、信頼できない PR コードがトークンを取得したり
  キャッシュに書き込めたりする経路。

呼び出し側が検証すべき入力に対してライブラリが期待通りに振る舞わない、という報告は
セキュリティ報告ではなく通常の issue として歓迎します。
