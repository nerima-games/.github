# nerima-games `docs/` 標準

本書は `nerima-games` 組織（16 リポジトリ）を横断して、各リポジトリの `docs/` フォルダが
**満たすべき「型」だけ**を定める。

**この標準が定めるのは構成と構造だけであり、内容ではない。**
`docs/*.md` の内容は今までどおり各リポジトリが個別に執筆し、所有し続ける。
org 横断で標準化するのは「どんなページが要るか」と「各ページが何のセクションを持つべきか」の 2 点のみで、
そこに何を書くかは各リポジトリの判断に委ねる（org 横断の型だけ標準化する、内容は集約しない）。

検証は 2026-08 時点で以下のリポジトリの `docs/` を実際に読んで行った:
`mc-kernel`（README.md / architecture.md / responsibility.md / public-api.md / testing.md / versioning.md / design-notes.md を通読）、
`mc-noise`（porting.md を通読）、`mx-ui`、`mc-dev-meta`（README.md を通読）、および
`mc-audio` / `mc-compose` / `mc-meshing` / `mc-physics` / `mc-playground-kit` / `mc-render` / `mc-save` / `mc-sim` /
`mc-worldgen` / `mx-gameplay` / `mx-multiplayer` / `mx-redstone` の `docs/` 一覧。

## 1. 前提: 16 リポジトリ構成

`nerima-games` は 4 階層 + 合成 + 開発ツールの 16 リポジトリから成る（各リポジトリの `docs/architecture.md` §1 が典拠）。

| 階層 | リポジトリ |
| --- | --- |
| 安定ライブラリ | `mc-kernel` / `mc-noise` / `mc-meshing` / `mc-physics` / `mc-save` / `mc-audio` |
| 基盤 | `mc-worldgen` / `mc-sim` / `mc-render` / `mc-playground-kit` |
| 体験モジュール | `mx-gameplay` / `mx-redstone` / `mx-ui` / `mx-multiplayer` |
| 合成 | `mc-compose` |
| 開発ツール（依存グラフ外） | `mc-dev-meta` |

`mc-kernel` は全リポジトリが import してよい唯一のリポジトリであり、他のどこにも依存しない。
`mc-dev-meta` は 15 リポジトリを `repos/` に clone して 1 つの pnpm workspace として束ねる薄いリポジトリで、
実行時の依存グラフには載らない（グラフ外）。この 2 点は本書の §2 と §3 の例外規定の前提になる。

## 2. 必須ページ一覧

以下は（`mc-dev-meta` を除く）ほぼ全リポジトリに共通して存在することを確認したページである。

| ページ | 役割 |
| --- | --- |
| `README.md` | `docs/` 全体の索引 + 読む順序 |
| `architecture.md` | 4 階層アーキテクチャ上の自リポジトリの位置 + 全 16 リポジトリの依存グラフ |
| `responsibility.md` | 自リポジトリの責務の範囲 + 明示的な非スコープ |
| `public-api.md` | 公開 API 面の全体像 |
| `testing.md` | 検証要件・完成条件・カバレッジゲートの状態 |
| `versioning.md` | `0.x` → `1.0.0` の方針 |
| `design-notes.md` | 設計原則 + 参照実装の失敗の実測知見 |

この 7 ページは新規リポジトリを作る際のひな型として扱う。

### 2-1. `porting.md` は条件付き必須

`docs/porting.md`（移植元と実測 LOC を記録するページ）は、
**凍結された参照実装 `takeokunn/ts-minecraft` からコードを移植するリポジトリでは必須**である。

`porting.md` に要求される最低限の型:

- 移植元（`<reference-impl>` 相対パス）の明記
- **実測 LOC。`wc -l` 等で実際に測った数値であって、plan.md の見積もりをそのまま転記したものではない**
- plan.md の見積もりとの差分がある場合はその差分と、差分が生じた理由（数え方の違いか、誤りか）
- 移植した部分・しなかった部分の対応表と、しなかった理由

これは `mc-noise/docs/porting.md` が実践している型で、同ファイルは
「plan.md の LOC 見積もりは信頼できない。本書の数値はすべてこのリポジトリで `wc -l` を実行して確認したものである」
と明記した上で、plan.md が挙げた 756 行を実測 406 行（移植対象のみ）と 756 行（Port/Service 込みの内訳再現）に切り分けている。
見積もりを疑わず転記するだけの `porting.md` は本標準を満たさない。

### 2-2. `mc-kernel` は `porting.md` を持たない — 意図的な適用除外

**`mc-kernel` に `porting.md` が無いのは標準からの逸脱ではなく、意図的な適用除外である。**

理由: 他の 14 実装リポジトリ（`mc-noise` 等）は参照実装の**特定の 1 パッケージ・1 ファイル群**を移植元として
指し示せるのに対し、`mc-kernel` は「全リポジトリが共有する語彙」を、参照実装の**多数の箇所**
（`block-type.ts` の 120 リテラル、`block-capabilities` 系の各判定散在箇所、`camera.ts`、`clock` まわり等）
を横断して実測・監査した上で**合成**したものである。`mc-kernel/docs/design-notes.md` §1 の
名指し判定の実測（90/38、307/77 など複数の数え方を比較検討している）や、
`mc-kernel/docs/responsibility.md` §3-2 が複数の参照実装ファイル
（`block-collision-predicates.ts` / `environment-hazard.config.ts` / `block-support.ts` /
`spawn-selection-search.ts` / `village-placement-surface.ts` など）を横断して語彙を導出している記述が、
この「1 対 1 の移植ではない」という性質を裏づけている。
`porting.md` の「移植元 → 実測 LOC」という型は、移植元が単一の識別可能なモジュールであることを前提にしており、
`mc-kernel` のような合成的な語彙集約リポジトリには当てはまらない。

したがって `porting.md` の欠如は conformance gap（標準違反）ではなく、リポジトリの性質に基づく正当な適用除外として扱う。

### 2-3. `mc-dev-meta` は標準ページ集合そのものが適用除外

`mc-dev-meta` は 4 階層の依存グラフに載らない開発ツールリポジトリであり、
`docs/README.md` が「他の 15 リポジトリは `docs/porting.md` と `docs/design-notes.md` を持つが、
mc-dev-meta には無い。参照実装 `takeokunn/ts-minecraft` に対応物が存在しないため
（あちらは 1 リポジトリ 11 パッケージのモノレポであり、束ねる道具そのものが必要なかった）」
と明記しているとおり、`porting.md` / `design-notes.md` の適用除外が公式に宣言されている。

代わりに `mc-dev-meta` は独自ページ集合を持つ:

| ページ | 役割 |
| --- | --- |
| `workflow.md` | 開発ワークフロー（セットアップ、日々の流れ） |
| `manifest.md` | `repos.json` が存在する理由、`pnpm sync` が壊さないものの説明 |
| `step2-status.md` | plan.md の Step 進捗状況の単一の追跡先 |

これは 16 リポジトリ構成の外側にある性質上の違いであり、他リポジトリがこの集合を真似る必要はない。

## 3. 各必須ページの構造要件（型のみ。内容は規定しない）

以下はページ内に**必ず存在すべきセクションの形**であり、各セクションに何を書くかは規定しない。
見出し文言の完全一致までは求めないが、読者がこの型を期待して開けることを標準とする。

### 3-1. `README.md`

- 自リポジトリが 16 リポジトリ構成のどこに位置するかの一文（例: 「他の 15 リポジトリすべてが import してよい唯一のリポジトリ」）
- 表記の凡例表（`<reference-impl>` や `plan.md` など、org 共通の記法を使う場合）
- **索引表**: `docs/` 配下の各ファイルへのリンク・内容の要約・主な読者を列挙した表
- **読む順序**: 目的別（「初めて使う」「何かを追加したい」「1.0.0 を切りたい」等）に読むべきページの順序を示すセクション
- （任意）調査報告書的な性質を持つページがあれば、他ページと性質が違う旨を明記するセクション

### 3-2. `architecture.md`

- 4 階層のどこに自リポジトリが属するかの表
- 全 16 リポジトリの依存グラフ（mermaid 等の図。実行時依存と devDependency 専用依存を区別する）
- 自リポジトリに関わる横断的な設計ルール（名詞/動詞ルール、devDependency 専用ルール、stage 順序の所有者など、
  org 共通ルールのうち自リポジトリに関係する部分の引用または要約）

### 3-3. `responsibility.md`

- **責務**: 何を所有するかの一覧（型・データ構造・Port など）
- **内部構成**: ディレクトリ構成と各ファイルの役割
- **非スコープ（明示）**: 「持ってはならないもの」を項目立てて列挙し、それぞれになぜ持たないか・どこが持つかを添えるセクション

### 3-4. `public-api.md`

- 公開バレル（エントリポイント）の説明
- 公開面の型・関数の一覧
- **各要素について「なぜこのリポジトリに置くのか」の説明**（横断的に使われる語彙であること、
  どこか一箇所に置くと依存グラフが壊れることなど、置き場所の判断根拠を明示するセクション）

### 3-5. `testing.md`

- **検証要件**: 上位仕様（plan.md 等）が定める検証方法の引用
- **コマンド**: 実行すべき検証コマンドの一覧と役割
- **カバレッジゲートの状態**: ゲートが有効か・閾値は何か・現在の実測値がどうかを明示するセクション
  （「まだ入れていない」場合はいつ入れるかの条件も含む）
- **完成条件**: 「完成」と呼べる条件を表形式などで列挙し、各条件の充足状況を示すセクション

### 3-6. `versioning.md`

- **現状**: 現在のバージョンと bump 理由
- **`0.x` に留める方針**: いつ `1.0.0` に上げるか、その前提条件
- **公開先**: パッケージレジストリ等の公開設定
- **bump の判断基準**: 変更の種類ごとに MAJOR/MINOR/PATCH（`0.x` の間の読み替えを含む）を対応づける表

### 3-7. `design-notes.md`

- **参照実装の失敗の実測**: 参照実装で問題になった設計・実装の実測結果（該当する場合は plan.md の見積もりとの照合を含む）
- **その失敗がなぜ致命的だったか**の説明
- **新実装での対処**: 同じ失敗を構造的に再発させないための設計原則・不変条件
- 該当する場合、その不変条件を保証する回帰テストへの参照

### 3-8. `porting.md`（条件付き必須ページ、§2-1 参照）

- 移植元の参照実装パスと計測日・計測方法
- 実測 LOC（`wc -l` 等の実行結果）と plan.md 見積もりとの突き合わせ
- 移植した/しなかった対応表
- 移植すべきテスト資産の対応表

## 4. リポジトリ固有の追加ページ

上記は「必須ページ」の下限であり、上限ではない。各リポジトリは必要に応じて独自ページを追加してよい。
確認できた例:

| リポジトリ | 追加ページ | 性質 |
| --- | --- | --- |
| `mc-kernel` | `capability-flag-audit.md` | 参照実装を実測した能力フラグ監査（一次資料） |
| `mc-kernel` | `freeze-checklist.md` | API を 1.0.0 で凍結する前の前提条件チェックリスト |
| `mx-ui` | `dom-oracle-triage.md` | DOM オラクル関連のトリアージ資料 |
| `mc-compose` | `e2e-triage.md` | E2E シナリオのトリアージ資料 |

**これらは当該リポジトリ固有の正当な追加であって、標準が要求するものではない。**
他のリポジトリがこれらのページ名を真似て複製する必要はない。追加ページが必要かどうかは各リポジトリの実情で判断する。

## 5. 本標準が定めないこと

- 各ページの**具体的な文章・数値・判断内容**。それらは各リポジトリの `docs/` が今までどおり所有し、
  本リポジトリ（`nerima-games/.github`）に集約・転記しない。
- ページの正確な行数や詳細さのレベル。実際に `testing.md` の行数は 173 行（`mc-save`）から
  795 行（`mc-worldgen`）まで広く分布しており、本標準はこの幅を許容する。
- セクション見出しの文言そのものの統一。§3 が定めるのは「その内容のまとまりが存在すること」であり、
  見出し文字列の一致ではない。

この標準への追従状況を機械検査する CI や lint は、本書の時点では存在しない。

## 6. 執筆言語

`docs/*.md` は日本語で書く。これは新しく課すルールではなく、`nerima-lisp/.github` および
`nerima-games` の全リポジトリの既存 `docs/*.md` が既に例外なく実践している慣行を、
本書が追認しているだけである。
